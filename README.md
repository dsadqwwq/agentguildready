# Agent Guild — Solana (Vercel OK)

Next.js 14 + Tailwind + Solana wallet adapter + Recharts.
- Relative imports (no alias)
- @solana/web3.js ^1.98.0 (satisfies wallet-adapter peer deps)

## Run locally
npm i
npm run dev

## Deploy on Vercel
1) Push this folder to a new GitHub repo
2) Import the repo on vercel.com
3) Build command: next build (default)
